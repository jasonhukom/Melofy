/* firebaseConfig.js — replace with YOUR Firebase project's own web config
   (Firebase Console -> Project settings -> General -> Your apps -> Web app
   -> SDK setup and configuration). Unlike the YouTube API key, this object
   is DESIGNED to be public/client-side — Firebase protects your project
   with Security Rules and the "Authorized domains" list, not by hiding
   this config. Safe to commit. */
export const firebaseConfig = {
  apiKey: "REPLACE_WITH_YOUR_FIREBASE_API_KEY",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "000000000000",
  appId: "1:000000000000:web:xxxxxxxxxxxxxxxxxxxxxx"
};
